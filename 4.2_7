#include <stdio.h>

int main(void)
{
    int n, t;
    t = 0;
    
    scanf("%d", &n);
    
    for(int f = 1; f < n; ++f) {
        printf("f = %d\n", f);
        f % 3 == 0 ? t += f : 0 || 
        f % 5 == 0 ? t += f : 0;
        printf("t = %d\n", t);
    }
    
    printf("res = %d ", t);
    
    return 0;
}
